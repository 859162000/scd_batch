<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.scd.batch.statistics.dao.trans.RechargeLDao">

    <select id="selectRechargeSumByDate" resultType="java.lang.Double">
        SELECT
        SUM(amt) AS amount
        FROM recharge_l
        WHERE
        <![CDATA[
              status IN #{statusList, jdbcType=VARCHAR}
              AND start_time >= #{startDate, jdbcType=TIMESTAMP}
              AND start_time <= #{endDate, jdbcType=TIMESTAMP}
        ]]>
    </select>

</mapper>